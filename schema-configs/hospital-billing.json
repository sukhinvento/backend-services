{
  "fields": [
    { "field_id": "invoice_number", "label": "Invoice Number", "type": "string", "required": true },
    { "field_id": "patient_id", "label": "Patient ID", "type": "string", "required": true },
    { "field_id": "admission_id", "label": "Admission ID", "type": "string", "required": false },
    { "field_id": "issued_date", "label": "Issued Date", "type": "date", "required": true },
    { "field_id": "due_date", "label": "Due Date", "type": "date", "required": true },
    { "field_id": "status", "label": "Status", "type": "enum", "values": ["Draft", "Issued", "Partially Paid", "Paid", "Cancelled", "Overdue"], "required": true },
    { "field_id": "subtotal", "label": "Subtotal (₹)", "type": "string", "required": true },
    { "field_id": "total_tax", "label": "Tax Amount (₹)", "type": "string", "required": true },
    { "field_id": "total_discount", "label": "Discount (₹)", "type": "string", "required": false },
    { "field_id": "grand_total", "label": "Grand Total (₹)", "type": "string", "required": true },
    { "field_id": "paid_amount", "label": "Paid Amount (₹)", "type": "string", "required": true },
    { "field_id": "payment_method", "label": "Payment Method", "type": "enum", "values": ["Cash", "Credit Card", "Bank Transfer", "Cheque", "UPI", "Insurance"], "required": false },
    { "field_id": "notes", "label": "Notes", "type": "string", "required": false }
  ]
}
